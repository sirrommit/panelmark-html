def get_base_css() -> str:
    """Return the base CSS string for panelmark-html layouts.

    Phase 3 will populate this with flexbox rules for splits, panel borders,
    headings, and spacing. Returns an empty string until then.
    """
    return ''
